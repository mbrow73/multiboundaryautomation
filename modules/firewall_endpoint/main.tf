/*
 * Placeholder module for the firewall endpoint.  In the original
 * inetconfig repository this module created a network firewall
 * endpoint and associated forwarding rules.  It remains here as a
 * stub so that users may extend it if needed.
 */

locals {}